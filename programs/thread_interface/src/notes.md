wrap all function calls to trap returns

lock is easy when yield is implemented

hard part - mechanics of basic scheduling, creating and yielding


global main thread

```{.python}
unmthread_t *thread { }
thread blah = &thread
```

